<%= form_with(model: dinosaur, local: true) do |form| %>
  <% if dinosaur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dinosaur.errors.count, "error") %> prohibited this dinosaur from being saved:</h2>

      <ul>
        <% dinosaur.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.number_field :height %>
  </div>

  <div class="field">
    <%= form.label :is_cool %>
    <%= form.check_box :is_cool %>
  </div>

  <div class="field">
    <%= form.label :b_day %>
    <%= form.datetime_select :b_day %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
